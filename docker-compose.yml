services:

  postgres:
    image: postgres:16

    container_name: licitaciones-db

    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
      POSTGRES_DB: licitaciones

    ports:
      - "5432:5432"

    volumes:
      - postgres_data:/var/lib/postgresql/data

    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U admin -d licitaciones"]
      interval: 5s
      timeout: 5s
      retries: 5


volumes:

  postgres_data: